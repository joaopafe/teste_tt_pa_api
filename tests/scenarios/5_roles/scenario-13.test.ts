describe.skip("Cenário 13 - Cadastro de role (POST /roles)", () => {
  test("13.1 - Cadastrar role por meio de requisição válida", () => {});

  test("13.2 - Retornar erro para cadastro sem autenticação", () => {});

  test("13.3 - Retornar erro de formato para cadastro sem todos os parâmetros obrigatórios", () => {});
});
