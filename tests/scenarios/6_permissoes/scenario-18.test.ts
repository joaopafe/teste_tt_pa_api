describe.skip("Cenário 18 - Cadastro de permissão (POST /permissoes)", () => {
  test("18.1 - Cadastrar permissão por meio de requisição válida", () => {});

  test("18.2 - Retornar erro para cadastro sem autenticação", () => {});

  test("18.3 - Retornar erro de formato para cadastro de permissão sem todos os parâmetros obrigatórios", () => {});
});
